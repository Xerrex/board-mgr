version: '3.7'

services:
    web:
        build: .
        command: python manage.py runserver 0.0.0.0:8000
        volumes:
            - .:/app/
        ports:
            - 8001:8000 #host:container
        env_file:
            - ./.env_dev
    
    